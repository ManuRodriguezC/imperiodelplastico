---
const { nombre, descripcion, imagen, slug, medidas } = Astro.props;
---
<div class="w-full flex flex-col md:flex-row gap-10 justify-around items-center mt-10">
  <div 
    class="w-[300px] h-[300px] lg:w-[400px] lg:h-[400px] xl:w-[500px] xl:h-[500px] 
      flex justify-center items-center shadow-2xl border-product-2 bg-gray-300">
    <img src={imagen} alt={nombre} class="w-[450px] h-auto">
  </div>
  <div class="flex flex-col items-center justify-center">
    <h2 class="text-black text-5xl text-center">{nombre}</h2>
    <p class="text-gray-500 font-serif py-4 text-center">{descripcion}</p>
    <p class="text-red-700 font-serif">Medidas: {medidas}</p>
  </div>
</div>
