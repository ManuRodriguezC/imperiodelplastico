---
import { Image } from 'astro:assets';
import { sections } from '../dataSources/sections';
---

<div class="w-full h-screen flex flex-col justify-center items-center gap-20">
  <h3 class="w-[800px] text-center text-xl">
		En <strong class="text-red-700 text-2xl">Imperios del Plástico</strong>, contamos con una amplia variedad de productos en distintas categorías, ofreciendo soluciones 
		resistentes y funcionales para satisfacer tus necesidades con la mejor calidad.
	</h3>
  <div class="flex flex-row gap-10">
		{
			sections.map((section) => (
				<div
					class={`relative group flex flex-col justify-end w-56 h-72 hover:shadow-2xl cursor-pointer
						shadow-back animate-move opacity-0 hover:scale-105 hover:-translate-y-1 transition-all duration-200
						 ${section.class}`} style={`${section.style}`}>
					<div class="absolute w-full h-full top-0 mask-fade-bottom rounded-md group-hover:shadow-2xl
            bg-red-100/50 border-2 border-red-100 transition-all duration-300"></div>

					<div class="absolute flex flex-col justify-between items-center p-2 w-full h-full top-0">
						<Image src={section.image} alt={section.name} height={200} width={100} />
						<div>
							<h3 class="mb-2 text-center text-red-700 text-xl font-bold">{section.name}</h3>
							<p class="text-sm text-center">{section.description}</p>
						</div>
					</div>
				</div>
			))
		}
  </div>
</div> 

<style>

	@keyframes moveIn {
    
		from {
			opacity: 0;
		} 
		to {
			transform: translateX(var(--end-x));
			opacity: 1;
		}
	}

	.animate-move {
    view-timeline-name: --image;
    view-timeline-axis: block;
    animation-timeline: --image;
    animation-name: moveIn;

    animation-range: entry 25% cover 30%;
    animation-fill-mode: forwards;
    animation-duration: 8s;
    animation-timing-function: linear;
	}

</style>
